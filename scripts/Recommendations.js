function recommendation_message() {
  var text;
  text = document.getElementById("text").value;

  document.getElementById("demo").innerHTML = "We will look into putting " + text + " onto our website!";
}
